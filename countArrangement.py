import numpy as np

for i in range(n):
    item = np.random(.randint(1,n+1))
    


print(np.random.randint(1, 10))
def countArrangement(n):
    for i in range(n):
        list1 = np.random.randint(10, 10)

    (i + 1) % pre[i]== 0
    pre[i] % (i + 1) == 0
